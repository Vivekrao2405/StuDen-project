package com.studen.skill;

public enum IconType {
    /** iconSlug is a simple-icons.org slug — a recognizable brand/software logo. */
    BRAND,
    /** iconSlug is a lucide-react component name — a generic, non-brand concept icon. */
    LUCIDE
}
