package com.studen.executionserver;

// Mirrors com.studen.practical.CodingLanguage on the main backend. Deliberately duplicated (not a
// shared module) -- this app is a genuinely standalone deployable, see pom.xml's own comment.
public enum CodingLanguage {
    JAVA,
    PYTHON,
    C,
    CPP
}
